runtime: "container"
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
startCommand:
  type: "http"
  port: 8080
  path: "/mcp"
  configSchema:
    type: object
    properties: {}
    required: []
    additionalProperties: false
